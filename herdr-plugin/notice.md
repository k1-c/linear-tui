[linear-tui] No reply needed beyond "OK". The user reads and triages Linear in linear-tui. `linear-tui context` prints what they are looking at right now — the open issue, the list on screen, the row under their cursor — so "this issue" or "the top three" can be resolved without IDs. `linear-tui issue show|comment|status <ID>` and `linear-tui issue create --team <key> --title <text>` act on Linear with their credentials; add `--json` for JSON. Only move an issue to Done when told to.
